#!/usr/bin/env bash
# Install Cloud SQL Auth Proxy (optional but secure for connections)

wget https://dl.google.com/cloudsql/cloud_sql_proxy.linux.amd64 -O cloud_sql_proxy
chmod +x cloud_sql_proxy
./cloud_sql_proxy -instances=your-project:region:prestashop-db=tcp:3306 &
